# Activity Summary for 21/9/2026

## 12:21:13
The provided logs detail changes across four JavaScript files, all related to Adobe AppMeasurement (Omniture) for web analytics, specifically version 2.1.0. The files appear to be environment-specific configurations for an audiencies tracking module within a CCMA web application.

Here's a breakdown of the key information:

**File-Specific Updates and Timestamps:**

1.  **`modul-audiencies-omniture.aws_pre.js` (Timestamp: 21/9/2026, 11:23:18)**
    *   This file configures Adobe Analytics for a pre-production or development/test environment, indicated by `s_account = 'caccma'`.
    *   It initializes `s.visitor` directly using `Visitor.getInstance('99EC8F7954BD14A00A4C98BC@AdobeOrg')`.
    *   The `sendVisitorId` function directly uses `Visitor.AuthState` without an explicit `typeof` check.

2.  **`modul-audiencies-omniture.aws_pro.js` (Timestamp: 21/9/2026, 11:23:50)**
    *   Similar to `aws_pre.js`, but configured for the production environment with `s_account = 'caproccma'`.
    *   The `_tpDST` (Daylight Saving Time) configuration for years 2026-2035 uses double quotes around the values, a minor stylistic difference from `aws_pre.js` which uses single quotes for these specific entries.
    *   The `sendVisitorId` function is identical to `aws_pre.js`, lacking the `typeof` check found in later versions.

3.  **`modul-audiencies-omniture.aws_test.js` (Timestamp: 21/9/2026, 11:24:33)**
    *   Configured for a test environment, using `s_account = 'caccma'`.
    *   Introduces conditional logic for `s.visitor` initialization: if `auxAudiencia.overwriteCrossDomainMCIDAndAID` is true, `overwriteCrossDomainMCIDAndAID: true` is passed to `Visitor.getInstance()`. This suggests enhanced control over cross-domain tracking in the test environment.
    *   The `sendVisitorId` function adds a `typeof Visitor.AuthState != 'undefined'` check for robustness before attempting to use `Visitor.AuthState`.
    *   The code for the `omnitureEventManager` function appears to be truncated at the end of the file.

4.  **`modul-audiencies-omniture.js` (Timestamp: 21/9/2026, 11:24:47)**
    *   This file also uses `s_account = 'caccma'` and includes the conditional `s.visitor` initialization and the more robust `sendVisitorId` check, matching `aws_test.js`.
    *   A notable difference is in the list of `s.Media.trackVars` and `s.pt` (clearVars) where `eVar84`, `prop84`, and `prop68` are removed, and `prop69` is added. This indicates a specific adjustment in the variables being tracked or cleared for media events compared to the other files.
    *   The code for the `omnitureEventManager` function is truncated at the same line as `aws_test.js`.

**Patterns and Recurring Elements:**

*   **Adobe AppMeasurement Core:** All files utilize the Adobe AppMeasurement (Omniture) JavaScript library (version 2.1.0) for web analytics, indicating a consistent platform for data collection.
*   **Standard Configuration:** Common configurations like `charSet = 'UTF-8'`, `currencyCode = 'EUR'`, and various link tracking settings (`trackDownloadLinks`, `trackExternalLinks`, `linkInternalFilters` for `tv3.cat`, `ccma.cat`, etc.) are present and largely identical across all versions.
*   **Daylight Saving Time (DST) Definitions:** The `s._tpDST` object, containing DST dates from 2016 to 2035, is consistently defined in all files, though with minor formatting variations in the `aws_pro.js` file.
*   **`s_doPlugins` Function:** This function is a central part of all files, responsible for setting campaign and `eVar1` from query parameters, triggering page view (`event1`) and search (`event2`) events, and conditionally adding `eVar63`, `eVar71`, `eVar73` based on `window.globalEvars`.
*   **Media Tracking Module:** All files load and extensively configure the `s.Media` module for comprehensive media playback tracking. This includes defining a long list of `eVar` and `prop` variables to track, setting `s.Media.trackSeconds = 360`, and mapping context data to analytics variables/events. The `omnitureIni` and `omnitureEventManager` functions are crucial for managing media events (open, play, pause, stop, close).
*   **Common Utility Functions:** A set of helper functions (`avoidEmptyEVar`, `avoidEmptyNumberEVar`, `getViewPortPage`, `getQueryString`, `getQueryStringHash`, `getLocalStorage`, `getQueryStringTag`, `getFont`, `getClenaUrl`, `getTemporada`) are consistently included across all files, providing standardized ways to extract and format data for analytics.
*   **Environment Specificity:** The primary distinction between files (`aws_pre`, `aws_pro`, `aws_test`, `modul-audiencies-omniture`) is the `s_account` variable, which dictates the Adobe Analytics report suite for each deployment environment (e.g., `caccma` for non-production, `caproccma` for production).
*   **Evolving Robustness:** There is a clear pattern of enhancing robustness, particularly seen in `aws_test.js` and `modul-audiencies-omniture.js` with the conditional `s.visitor` initialization and the `typeof` check around `Visitor.AuthState` in `sendVisitorId`.
*   **Truncation:** The `aws_test.js` and `modul-audiencies-omniture.js` files are both truncated mid-function at the very end, suggesting either an incomplete commit or a limitation in the log capture process.

## 13:21:12
The provided log details changes across four JavaScript files, all related to Adobe AppMeasurement (Omniture) tracking for the `CCMA_V2` project, specifically within the `AudienciesBundle`. All timestamps are clustered around 11:23-11:24 AM on 21/9/2026, indicating a synchronized update or deployment.

**File-specific updates:**

1.  **`modul-audiencies-omniture.aws_pre.js`** (Timestamp: 21/9/2026, 11:23:18)
    *   This file configures Adobe Analytics for a pre-production or development/test environment.
    *   The `s_account` variable is set to `'caccma'`.
    *   The `s.visitor` instance is conditionally initialized to allow `overwriteCrossDomainMCIDAndAID: true` if `auxAudiencia.overwriteCrossDomainMCIDAndAID` is defined and true.
    *   The `sendVisitorId` function directly sets customer IDs without an initial `typeof Visitor.AuthState != 'undefined'` check.

2.  **`modul-audiencies-omniture.aws_pro.js`** (Timestamp: 21/9/2026, 11:23:50)
    *   This file is for the production environment.
    *   The primary distinction is the `s_account` variable, which is set to `'caproccma'`.
    *   The `s.visitor` instance is initialized without the conditional `overwriteCrossDomainMCIDAndAID` logic present in the `aws_pre` and `aws_test` versions.
    *   Similar to `aws_pre.js`, the `sendVisitorId` function lacks the `typeof Visitor.AuthState != 'undefined'` check.
    *   Minor stylistic change in the `s._tpDST` object for years 2026-2035, where strings use double quotes instead of single quotes.

3.  **`modul-audiencies-omniture.aws_test.js`** (Timestamp: 21/9/2026, 11:24:33)
    *   This file appears to be a test environment configuration.
    *   The `s_account` variable is `'caccma'`, similar to `aws_pre.js`.
    *   It includes the conditional `s.visitor` initialization logic using `overwriteCrossDomainMCIDAndAID`, matching `aws_pre.js`.
    *   A notable difference is the `sendVisitorId` function, which now includes a `typeof Visitor.AuthState != 'undefined'` check before attempting to set customer IDs, enhancing robustness.
    *   The provided log for this file is truncated, ending within the `omnitureEventManager` function.

4.  **`modul-audiencies-omniture.js`** (Timestamp: 21/9/2026, 11:24:47)
    *   This appears to be a general or default Omniture configuration.
    *   The `s_account` variable is `'caccma'`.
    *   It also includes the conditional `s.visitor` initialization logic.
    *   The `sendVisitorId` function includes the `typeof Visitor.AuthState != 'undefined'` check, similar to `aws_test.js`.
    *   There is a subtle but notable difference in the list of `eVar` and `prop` variables specified in `s.Media.trackVars` within the `omnitureIni` function, and subsequently cleared in `omnitureEventManager` via `s.pt()`. Specifically, `prop69` is included, while `eVar69` and `prop68` are adjusted compared to the `aws_` versions.
    *   The provided log for this file is also truncated at the exact same point in the `omnitureEventManager` function as `aws_test.js`.

**Patterns and Recurring Elements:**

*   **Adobe Analytics (Omniture) Implementation:** All files are implementations of Adobe AppMeasurement JavaScript version 2.1.0, designed for website analytics and media tracking.
*   **Environment-Specific Configuration:** A consistent pattern of having separate files for 'pre' (development/test), 'pro' (production), and 'test' environments, primarily distinguished by the `s_account` variable (e.g., `'caccma'` for dev/test, `'caproccma'` for production).
*   **Core Tracking Logic:** The `s_doPlugins` function, `omnitureIni` media tracking setup, and `omnitureEventManager` for media playback events are largely consistent across all files, suggesting a shared and standardized approach to data collection. This includes tracking campaign parameters, page views, search events, and various media events (open, play, pause, stop, close).
*   **Media Tracking Variables:** Extensive lists of `eVar` and `prop` variables (e.g., `eVar10` through `eVar89`, `prop10` through `prop89`, `list1`, `list2`) are consistently defined for media tracking.
*   **Utility Functions:** A set of common utility functions are present in all files to handle common tasks like avoiding empty eVars, detecting viewport size, parsing query strings, managing local storage for recommendations, and formatting URLs/tags.
*   **Daylight Saving Time (DST) Configuration:** All files include an identical `s._tpDST` object, mapping DST dates from 2016 to 2035, indicating a global configuration for time zone adjustments.
*   **Code Structure and Comments:** All files begin with the same Adobe copyright notice and AppMeasurement version, and share similar internal comment structures for configuration sections.
*   **Truncation:** Both `modul-audiencies-omniture.aws_test.js` and `modul-audiencies-omniture.js` are truncated at the same line within the `omnitureEventManager` function, which might indicate an incomplete log capture.