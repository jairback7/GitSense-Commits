# Activity Summary for 21/9/2026

## 12:21:13
The provided logs detail changes across four JavaScript files, all related to Adobe AppMeasurement (Omniture) for web analytics, specifically version 2.1.0. The files appear to be environment-specific configurations for an audiencies tracking module within a CCMA web application.

Here's a breakdown of the key information:

**File-Specific Updates and Timestamps:**

1.  **`modul-audiencies-omniture.aws_pre.js` (Timestamp: 21/9/2026, 11:23:18)**
    *   This file configures Adobe Analytics for a pre-production or development/test environment, indicated by `s_account = 'caccma'`.
    *   It initializes `s.visitor` directly using `Visitor.getInstance('99EC8F7954BD14A00A4C98BC@AdobeOrg')`.
    *   The `sendVisitorId` function directly uses `Visitor.AuthState` without an explicit `typeof` check.

2.  **`modul-audiencies-omniture.aws_pro.js` (Timestamp: 21/9/2026, 11:23:50)**
    *   Similar to `aws_pre.js`, but configured for the production environment with `s_account = 'caproccma'`.
    *   The `_tpDST` (Daylight Saving Time) configuration for years 2026-2035 uses double quotes around the values, a minor stylistic difference from `aws_pre.js` which uses single quotes for these specific entries.
    *   The `sendVisitorId` function is identical to `aws_pre.js`, lacking the `typeof` check found in later versions.

3.  **`modul-audiencies-omniture.aws_test.js` (Timestamp: 21/9/2026, 11:24:33)**
    *   Configured for a test environment, using `s_account = 'caccma'`.
    *   Introduces conditional logic for `s.visitor` initialization: if `auxAudiencia.overwriteCrossDomainMCIDAndAID` is true, `overwriteCrossDomainMCIDAndAID: true` is passed to `Visitor.getInstance()`. This suggests enhanced control over cross-domain tracking in the test environment.
    *   The `sendVisitorId` function adds a `typeof Visitor.AuthState != 'undefined'` check for robustness before attempting to use `Visitor.AuthState`.
    *   The code for the `omnitureEventManager` function appears to be truncated at the end of the file.

4.  **`modul-audiencies-omniture.js` (Timestamp: 21/9/2026, 11:24:47)**
    *   This file also uses `s_account = 'caccma'` and includes the conditional `s.visitor` initialization and the more robust `sendVisitorId` check, matching `aws_test.js`.
    *   A notable difference is in the list of `s.Media.trackVars` and `s.pt` (clearVars) where `eVar84`, `prop84`, and `prop68` are removed, and `prop69` is added. This indicates a specific adjustment in the variables being tracked or cleared for media events compared to the other files.
    *   The code for the `omnitureEventManager` function is truncated at the same line as `aws_test.js`.

**Patterns and Recurring Elements:**

*   **Adobe AppMeasurement Core:** All files utilize the Adobe AppMeasurement (Omniture) JavaScript library (version 2.1.0) for web analytics, indicating a consistent platform for data collection.
*   **Standard Configuration:** Common configurations like `charSet = 'UTF-8'`, `currencyCode = 'EUR'`, and various link tracking settings (`trackDownloadLinks`, `trackExternalLinks`, `linkInternalFilters` for `tv3.cat`, `ccma.cat`, etc.) are present and largely identical across all versions.
*   **Daylight Saving Time (DST) Definitions:** The `s._tpDST` object, containing DST dates from 2016 to 2035, is consistently defined in all files, though with minor formatting variations in the `aws_pro.js` file.
*   **`s_doPlugins` Function:** This function is a central part of all files, responsible for setting campaign and `eVar1` from query parameters, triggering page view (`event1`) and search (`event2`) events, and conditionally adding `eVar63`, `eVar71`, `eVar73` based on `window.globalEvars`.
*   **Media Tracking Module:** All files load and extensively configure the `s.Media` module for comprehensive media playback tracking. This includes defining a long list of `eVar` and `prop` variables to track, setting `s.Media.trackSeconds = 360`, and mapping context data to analytics variables/events. The `omnitureIni` and `omnitureEventManager` functions are crucial for managing media events (open, play, pause, stop, close).
*   **Common Utility Functions:** A set of helper functions (`avoidEmptyEVar`, `avoidEmptyNumberEVar`, `getViewPortPage`, `getQueryString`, `getQueryStringHash`, `getLocalStorage`, `getQueryStringTag`, `getFont`, `getClenaUrl`, `getTemporada`) are consistently included across all files, providing standardized ways to extract and format data for analytics.
*   **Environment Specificity:** The primary distinction between files (`aws_pre`, `aws_pro`, `aws_test`, `modul-audiencies-omniture`) is the `s_account` variable, which dictates the Adobe Analytics report suite for each deployment environment (e.g., `caccma` for non-production, `caproccma` for production).
*   **Evolving Robustness:** There is a clear pattern of enhancing robustness, particularly seen in `aws_test.js` and `modul-audiencies-omniture.js` with the conditional `s.visitor` initialization and the `typeof` check around `Visitor.AuthState` in `sendVisitorId`.
*   **Truncation:** The `aws_test.js` and `modul-audiencies-omniture.js` files are both truncated mid-function at the very end, suggesting either an incomplete commit or a limitation in the log capture process.

## 13:21:12
The provided log details changes across four JavaScript files, all related to Adobe AppMeasurement (Omniture) tracking for the `CCMA_V2` project, specifically within the `AudienciesBundle`. All timestamps are clustered around 11:23-11:24 AM on 21/9/2026, indicating a synchronized update or deployment.

**File-specific updates:**

1.  **`modul-audiencies-omniture.aws_pre.js`** (Timestamp: 21/9/2026, 11:23:18)
    *   This file configures Adobe Analytics for a pre-production or development/test environment.
    *   The `s_account` variable is set to `'caccma'`.
    *   The `s.visitor` instance is conditionally initialized to allow `overwriteCrossDomainMCIDAndAID: true` if `auxAudiencia.overwriteCrossDomainMCIDAndAID` is defined and true.
    *   The `sendVisitorId` function directly sets customer IDs without an initial `typeof Visitor.AuthState != 'undefined'` check.

2.  **`modul-audiencies-omniture.aws_pro.js`** (Timestamp: 21/9/2026, 11:23:50)
    *   This file is for the production environment.
    *   The primary distinction is the `s_account` variable, which is set to `'caproccma'`.
    *   The `s.visitor` instance is initialized without the conditional `overwriteCrossDomainMCIDAndAID` logic present in the `aws_pre` and `aws_test` versions.
    *   Similar to `aws_pre.js`, the `sendVisitorId` function lacks the `typeof Visitor.AuthState != 'undefined'` check.
    *   Minor stylistic change in the `s._tpDST` object for years 2026-2035, where strings use double quotes instead of single quotes.

3.  **`modul-audiencies-omniture.aws_test.js`** (Timestamp: 21/9/2026, 11:24:33)
    *   This file appears to be a test environment configuration.
    *   The `s_account` variable is `'caccma'`, similar to `aws_pre.js`.
    *   It includes the conditional `s.visitor` initialization logic using `overwriteCrossDomainMCIDAndAID`, matching `aws_pre.js`.
    *   A notable difference is the `sendVisitorId` function, which now includes a `typeof Visitor.AuthState != 'undefined'` check before attempting to set customer IDs, enhancing robustness.
    *   The provided log for this file is truncated, ending within the `omnitureEventManager` function.

4.  **`modul-audiencies-omniture.js`** (Timestamp: 21/9/2026, 11:24:47)
    *   This appears to be a general or default Omniture configuration.
    *   The `s_account` variable is `'caccma'`.
    *   It also includes the conditional `s.visitor` initialization logic.
    *   The `sendVisitorId` function includes the `typeof Visitor.AuthState != 'undefined'` check, similar to `aws_test.js`.
    *   There is a subtle but notable difference in the list of `eVar` and `prop` variables specified in `s.Media.trackVars` within the `omnitureIni` function, and subsequently cleared in `omnitureEventManager` via `s.pt()`. Specifically, `prop69` is included, while `eVar69` and `prop68` are adjusted compared to the `aws_` versions.
    *   The provided log for this file is also truncated at the exact same point in the `omnitureEventManager` function as `aws_test.js`.

**Patterns and Recurring Elements:**

*   **Adobe Analytics (Omniture) Implementation:** All files are implementations of Adobe AppMeasurement JavaScript version 2.1.0, designed for website analytics and media tracking.
*   **Environment-Specific Configuration:** A consistent pattern of having separate files for 'pre' (development/test), 'pro' (production), and 'test' environments, primarily distinguished by the `s_account` variable (e.g., `'caccma'` for dev/test, `'caproccma'` for production).
*   **Core Tracking Logic:** The `s_doPlugins` function, `omnitureIni` media tracking setup, and `omnitureEventManager` for media playback events are largely consistent across all files, suggesting a shared and standardized approach to data collection. This includes tracking campaign parameters, page views, search events, and various media events (open, play, pause, stop, close).
*   **Media Tracking Variables:** Extensive lists of `eVar` and `prop` variables (e.g., `eVar10` through `eVar89`, `prop10` through `prop89`, `list1`, `list2`) are consistently defined for media tracking.
*   **Utility Functions:** A set of common utility functions are present in all files to handle common tasks like avoiding empty eVars, detecting viewport size, parsing query strings, managing local storage for recommendations, and formatting URLs/tags.
*   **Daylight Saving Time (DST) Configuration:** All files include an identical `s._tpDST` object, mapping DST dates from 2016 to 2035, indicating a global configuration for time zone adjustments.
*   **Code Structure and Comments:** All files begin with the same Adobe copyright notice and AppMeasurement version, and share similar internal comment structures for configuration sections.
*   **Truncation:** Both `modul-audiencies-omniture.aws_test.js` and `modul-audiencies-omniture.js` are truncated at the same line within the `omnitureEventManager` function, which might indicate an incomplete log capture.

## 14:21:12
The changes log shows a series of rapid updates to Adobe AppMeasurement (Omniture) JavaScript files, all occurring on 21/9/2026 within a span of just over a minute. The files are organized by environment: `aws_pre.js`, `aws_pro.js`, `aws_test.js`, and a generic `modul-audiencies-omniture.js`.

**File-specific Updates:**

*   **`modul-audiencies-omniture.aws_pre.js` (21/9/2026, 11:23:18)**: This file uses `s_account = 'caccma'`, designated for DEV-TEST-PRE environments. It configures the `s.visitor` object with a basic `Visitor.getInstance()` call without specific options and includes `eVar84` in its media tracking and clear variables lists.
*   **`modul-audiencies-omniture.aws_pro.js` (21/9/2026, 11:23:50)**: This file is configured for production, setting `s_account = 'caproccma'`. Its `s.visitor` setup and media tracking variables are identical to `aws_pre.js`. There's a minor formatting difference in the `s._tpDST` object, where date strings for 2026-2035 are enclosed in double quotes.
*   **`modul-audiencies-omniture.aws_test.js` (21/9/2026, 11:24:33)**: This file reverts to `s_account = 'caccma'`. It introduces a significant change to the `s.visitor` configuration, conditionally adding `overwriteCrossDomainMCIDAndAID: true` to `Visitor.getInstance()` based on the `auxAudiencia` object. The `sendVisitorId` function is also updated to include a `typeof Visitor.AuthState != 'undefined'` check for robustness. The code for the `omnitureEventManager` function is truncated at the end.
*   **`modul-audiencies-omniture.js` (21/9/2026, 11:24:47)**: This file also uses `s_account = 'caccma'` and incorporates the same conditional `s.visitor` configuration and `sendVisitorId` check as `aws_test.js`. A key difference in this file is the modification of the media tracking variables: `eVar84` is removed from both `s.Media.trackVars` and the `s.pt('...', 'clearVars')` call. Additionally, `prop68` is removed from the `clearVars` list, and `prop69` is added to both `trackVars` and `clearVars`. Similar to `aws_test.js`, the `omnitureEventManager` function is truncated.

**Patterns and Recurring Elements:**

*   **Adobe Analytics (Omniture) Implementation:** All files are implementations of Adobe AppMeasurement for JavaScript, version 2.1.0, and share the core logic for initializing Omniture, defining standard configuration (e.g., `charSet`, `currencyCode`, link tracking), and implementing the `s_doPlugins` function for setting campaign, page view, and search events.
*   **Media Tracking:** All files include a comprehensive `omnitureIni` function that loads the Media module and defines extensive `trackVars` (eVars and props from 10 to 89, plus lists) and `contextDataMapping` for media events (open, play, pause, stop, close, milestones). The `omnitureEventManager` orchestrates media tracking events.
*   **Daylight Saving Time (DST) Configuration:** The `s._tpDST` object is consistently present across all files, defining DST dates up to 2035.
*   **Utility Functions:** A set of common utility functions like `avoidEmptyEVar`, `avoidEmptyNumberEVar`, `getViewPortPage`, `getQueryString`, `getQueryStringHash`, `getLocalStorage`, `getQueryStringTag`, `getFont`, `getClenaUrl`, and `getTemporada` are present in all files, indicating a shared helper library for data preparation before sending to Omniture.
*   **Environment Specificity:** The `s_account` variable clearly differentiates between `caccma` (DEV-TEST-PRE/general) and `caproccma` (PRO), indicating environment-specific deployments.
*   **Progressive Enhancements:** The `aws_test.js` and `modul-audiencies-omniture.js` files show iterative improvements by adding a conditional `overwriteCrossDomainMCIDAndAID` option for visitor tracking and a `Visitor.AuthState` check for `sendVisitorId`, suggesting increased sophistication in visitor identification and cross-domain tracking.
*   **Variable Schema Adjustments:** The change in `modul-audiencies-omniture.js` regarding `eVar84`, `prop68`, and `prop69` in media tracking indicates an ongoing refinement of the specific data points being collected and cleared for media analytics.

## 15:21:13
The provided log details changes across four JavaScript files, all related to Adobe AppMeasurement (Omniture) for audience and media tracking. All changes occurred on **21/9/2026**, within a short span, indicating a concentrated update or deployment.

**File-specific Updates:**

1.  **`modul-audiencies-omniture.aws_pre.js` (Timestamp: 11:23:18)**
    *   This file configures Adobe Omniture tracking for a "pre" environment, setting `s_account` to `'caccma'`.
    *   It initializes `s.visitor` using `Visitor.getInstance('99EC8F7954BD14A00A4C98BC@AdobeOrg')`.
    *   The script includes standard link tracking, conversion configuration, and defines Daylight Saving Time (`_tpDST`) rules up to 2035.
    *   The `s_doPlugins` function handles campaign tracking, page view (`event1`), search (`event2`), and conditionally passes `eVar63, eVar71, eVar73` as properties.
    *   It checks for the presence of the Marketing Cloud ID Service and sets `pageType` for error pages.
    *   The `omnitureIni` function specifies a comprehensive list of `eVar` and `prop` variables for media tracking, along with media event mapping. `s.Media.trackSeconds` is set to 360.
    *   A `omnitureEventManager` function manages media lifecycle events (OPEN, PLAY, PAUSE, STOP, CLOSE), clearing a fixed list of `eVar` and `prop` variables on media close, citing "IPTV - EQPUB-1935".

2.  **`modul-audiencies-omniture.aws_pro.js` (Timestamp: 11:23:50)**
    *   This file is nearly identical to `aws_pre.js`, but it's configured for a "pro" (production) environment.
    *   The primary difference is the `s_account` variable, which is set to `'caproccma'`.
    *   A minor, non-functional formatting change is present in the `_tpDST` definition for the year 2026, where the date string is quoted.

3.  **`modul-audiencies-omniture.aws_test.js` (Timestamp: 11:24:33)**
    *   This file shares most of its content with `aws_pre.js`, using `s_account = 'caccma'`.
    *   A significant change is introduced in the `s.visitor` initialization: it now conditionally checks for `auxAudiencia.overwriteCrossDomainMCIDAndAID` to potentially pass an `overwriteCrossDomainMCIDAndAID: true` option to `Visitor.getInstance`.
    *   The `sendVisitorId` function is enhanced with an additional `typeof Visitor.AuthState != 'undefined'` check for robustness.
    *   The provided code for this file is **truncated** at the end of the `omnitureEventManager` function.

4.  **`modul-audiencies-omniture.js` (Timestamp: 11:24:47)**
    *   This file appears to be a generic or default version, also using `s_account = 'caccma'`.
    *   It incorporates the same conditional `s.visitor` initialization logic and the `sendVisitorId` robustness check found in `aws_test.js`.
    *   A key update in this file is the modification of tracked variables: `eVar84` and `prop68` are removed from the `s.Media.trackVars` list and the variable clearing list in `omnitureEventManager`, while `prop69` is added to `s.Media.trackVars`. This indicates a refinement in the data points being collected or reset.
    *   Similar to `aws_test.js`, this file's content is also **truncated** at the end of the `omnitureEventManager` function.

**Patterns and Recurring Elements:**

*   **Adobe AppMeasurement:** All files are implementations of Adobe AppMeasurement for JavaScript version 2.1.0, focusing on web and media analytics.
*   **Environmental Configuration:** A clear pattern of `s_account` variable distinction exists, differentiating between production (`caproccma`) and non-production (`caccma`) environments.
*   **Media Tracking:** All files extensively configure Adobe Media Analytics, tracking numerous `eVar` and `prop` variables, as well as specific media events (e.g., `event13` for time played, `event16` for media view, and milestones at 10%, 30%, 60%, 90%).
*   **Utility Functions:** A consistent set of utility functions (`avoidEmptyEVar`, `avoidEmptyNumberEVar`, `getViewPortPage`, `getQueryString`, `getQueryStringHash`, `getLocalStorage`, `sendVisitorId`, `getQueryStringTag`, `getFont`, `getClenaUrl`, `getTemporada`) is present across all files, indicating shared logic for data manipulation and extraction.
*   **Conditional Visitor API Initialization:** The `aws_test.js` and `modul-audiencies-omniture.js` files introduce a pattern of conditionally setting `overwriteCrossDomainMCIDAndAID` for the `Visitor.getInstance` call, suggesting a feature or bug fix related to cross-domain tracking was implemented in these versions.
*   **Variable Refinement:** The updates to `s.Media.trackVars` and the `s.pt` clearVars list in `modul-audiencies-omniture.js` indicate an ongoing process of optimizing or adjusting the data collected by Omniture.
*   **Code Truncation:** A recurring issue in the provided log is that `aws_test.js` and `modul-audiencies-omniture.js` are incomplete, cutting off mid-function.

## 16:21:11
The provided code log details changes made to Adobe Analytics (Omniture) tracking JavaScript files, all occurring on 21/9/2026 within a short timeframe. These files are located in a `www.ccma.cat` resource path, specifically related to an "AudienciesBundle".

**File-Specific Updates:**

*   **`modul-audiencies-omniture.aws_pre.js` (Timestamp: 11:23:18):**
    *   Configures Adobe Analytics for a "DEV-TEST-PRE" environment, identified by `s_account = 'caccma'`.
    *   Initializes the `s.visitor` object directly using `Visitor.getInstance('99EC8F7954BD14A00A4C98BC@AdobeOrg')`.
    *   Defines a comprehensive list of `eVar` and `prop` variables for media tracking within `s.Media.trackVars`.
    *   The `sendVisitorId` function directly calls `visitor.setCustomerIDs` without an explicit `Visitor.AuthState` type check.

*   **`modul-audiencies-omniture.aws_pro.js` (Timestamp: 11:23:50):**
    *   This file is nearly identical to `aws_pre.js` but configures Adobe Analytics for a "PRO" (production) environment, setting `s_account = 'caproccma'`.
    *   It shares the same direct `s.visitor` initialization, extensive `s.Media.trackVars` list, and `sendVisitorId` implementation as `aws_pre.js`.

*   **`modul-audiencies-omniture.aws_test.js` (Timestamp: 11:24:33):**
    *   Configures for a "DEV-TEST-PRE" environment (`s_account = 'caccma'`).
    *   **Introduces a significant change in `s.visitor` initialization:** It now conditionally sets `overwriteCrossDomainMCIDAndAID: true` based on the `auxAudiencia.overwriteCrossDomainMCIDAndAID` variable. This suggests an enhancement for managing cross-domain tracking.
    *   The `sendVisitorId` function is updated to include a check `if (typeof Visitor.AuthState != 'undefined')` before attempting to set customer IDs, making it more robust.
    *   The list of `s.Media.trackVars` remains consistent with the `aws_pre/pro` versions.

*   **`modul-audiencies-omniture.js` (Timestamp: 11:24:47):**
    *   Also configures for a "DEV-TEST-PRE" environment (`s_account = 'caccma'`).
    *   Includes the same conditional `s.visitor` initialization and `sendVisitorId` robustness check as `aws_test.js`.
    *   **Features a distinct change in `s.Media.trackVars`:** Compared to the `aws_pre/pro/test` versions, this file's `s.Media.trackVars` list omits `eVar84` and `prop68` while adding `prop69`. This indicates a specific refinement in the set of variables being tracked for media events in this default/primary script.

**Patterns and Recurring Elements:**

*   **Environment-Specific Configuration:** The filenames (`aws_pre`, `aws_pro`, `aws_test`) and the `s_account` variable's value consistently reflect different deployment environments (DEV-TEST-PRE and PRO).
*   **Adobe Analytics Core:** All files utilize "AppMeasurement for JavaScript version: 2.1.0" and implement standard Adobe Analytics features like link tracking, conversion configuration, and media tracking via the `s.Media` module.
*   **Media Tracking Logic:** The `omnitureIni` and `omnitureEventManager` functions are consistently defined across all files, handling media events (OPEN, PLAY, PAUSE/STOP, CLOSE) and clearing a wide array of `eVar` and `prop` variables upon media close.
*   **Daylight Saving Time (DST) Configuration:** All files contain an identical `s._tpDST` object, providing DST dates from 2016 through 2035.
*   **Common Utility Functions:** Helper functions like `avoidEmptyEVar`, `getViewPortPage`, `getQueryString`, `getLocalStorage`, `getQueryStringHash`, `getQueryStringTag`, `getFont`, `getClenaUrl`, and `getTemporada` are present and largely identical across all versions.
*   **Timestamp Proximity:** All modifications occurred within a minute on the same day, suggesting a unified deployment or a rapid sequence of related updates.

In summary, the changes primarily involve environment-specific account IDs, an introduction of conditional logic for cross-domain visitor ID tracking (in `aws_test.js` and `modul-audiencies-omniture.js`), improved robustness in setting visitor IDs, and a slight adjustment in the media tracking variables for the primary `modul-audiencies-omniture.js` file.