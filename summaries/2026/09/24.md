# Activity Summary for 24/9/2026

## 9:49:05
The `next.config.js` file, updated on 24/9/2026 at 9:30:46, received extensive modifications focused on build configuration, environment handling, and client-side compatibility.

Key updates include:

*   **Environment Configuration:** `next.config.js` now dynamically sets `isDev`, `isExport`, `proxyProtocol`, `proxyHost`, and `proxyPort` based on `next/constants` and `process.env` variables, making proxy settings conditional for development environments.
*   **Caching Headers:** `Cache-Control` headers are configured for `/_next/static/:path*` to enable public, immutable caching for one year.
*   **Image Handling:** A custom image loader (`./imageLoader.js`) is specified for image optimization.
*   **General Next.js Settings:** `reactStrictMode`, `poweredByHeader`, and `compress` are disabled, while `trailingSlash` is enabled. The `basePath` is set to `/apptv`, and `productionBrowserSourceMaps` are enabled.
*   **Transpilation & Experimentation:** Several internal packages (`@portal-internet/bff`, `@portal-internet/core`, `@portal-internet/components`) along with `debug` and `jwt-decode` are explicitly listed for transpilation. `experimental.esmExternals` is set to `'loose'`.
*   **Webpack Enhancements (Client-side Focus):**
    *   `watchOptions` for polling are conditionally enabled.
    *   `config.resolve.fallback` is expanded to prevent bundling Node.js core modules (`fs`, `http2`, `zlib`, `http`, `https`, `stream`, `crypto`, `path`, `vm`, `net`, `tls`) on the client.
    *   A significant update forces `axios` to use its browser-specific build (`axios/dist/browser/axios.cjs`) on the client to avoid issues with its Node.js adapter, using both `config.resolve.alias` and a `webpack.NormalModuleReplacementPlugin`.
    *   The `webpack.output.environment` is explicitly configured to emit ES5 syntax by setting properties like `arrowFunction`, `const`, `destructuring`, `forOf`, `optionalChaining`, and `templateLiteral` to `false`.
    *   An `ES5DownlevelPlugin` is introduced to post-process all client-side JavaScript assets through SWC, ensuring any remaining ES6+ syntax is converted to ES5.

A recurring pattern in these changes is a strong emphasis on **ensuring backward compatibility and broad browser support** by explicitly downleveling JavaScript to ES5 for client-side builds and preventing Node.js-specific modules from being bundled on the client. This suggests a target environment that may not fully support modern JavaScript features.