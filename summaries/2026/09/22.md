# Activity Summary for 22/9/2026

## 2:40:40
The provided logs detail updates to several Adobe AppMeasurement (Omniture) JavaScript files, all occurring on **21/9/2026** within a short period (between **11:23:18** and **11:24:47**), suggesting a coordinated deployment or update.

**File-Specific Updates:**

1.  **`modul-audiencies-omniture.aws_pre.js` (Timestamp: 11:23:18)** and **`modul-audiencies-omniture.aws_pro.js` (Timestamp: 11:23:50)**:
    *   These two files are largely identical except for the `s_account` variable.
        *   `aws_pre.js` uses `'caccma'` for development/testing/pre-production environments.
        *   `aws_pro.js` uses `'caproccma'` for the production environment.
    *   They define the Adobe Visitor ID service (`s.visitor`) without any special `overwriteCrossDomainMCIDAndAID` options.
    *   The `sendVisitorId` function directly calls `visitor.setCustomerIDs` after checking if `idUser` is not empty.
    *   The lists of variables tracked (`s.Media.trackVars`) and cleared (`s.pt`) consistently include `eVar83, eVar84, eVar86, eVar87, eVar88, eVar89` and `prop86, prop87, prop88, prop89`, along with `eVar68, eVar69` and `prop68`.

2.  **`modul-audiencies-omniture.aws_test.js` (Timestamp: 11:24:33)** and **`modul-audiencies-omniture.js` (Timestamp: 11:24:47)**:
    *   These files introduce two key functional enhancements compared to the `aws_pre.js` and `aws_pro.js` versions:
        1.  **Conditional Cross-Domain Tracking**: The initialization of the `s.visitor` object now conditionally includes `overwriteCrossDomainMCIDAndAID: true` based on the value of `auxAudiencia.overwriteCrossDomainMCIDAndAID`. This provides more granular control over Adobe Marketing Cloud ID Service behavior.
        2.  **Enhanced `sendVisitorId` Robustness**: The `sendVisitorId` function is updated to first check `typeof Visitor.AuthState != 'undefined'` before attempting to set customer IDs, preventing potential runtime errors if the `Visitor.AuthState` object is not available.
    *   `modul-audiencies-omniture.aws_test.js` maintains the `s_account = 'caccma'` value.
    *   `modul-audiencies-omniture.js` also uses `s_account = 'caccma'`.
    *   **Inconsistency in `modul-audiencies-omniture.js`**: While inheriting the two key enhancements, this specific file shows a slight inconsistency in its variable tracking lists:
        *   `s.Media.trackVars` for `modul-audiencies-omniture.js` explicitly removes `eVar84` and `prop68` (while adding `prop69`) compared to the `aws_*.js` files.
        *   However, the `s.pt` call (used for clearing variables) in `modul-audiencies-omniture.js` still includes `eVar84` and `prop68` (but not `prop69`) in its list of variables to clear. This suggests a potential oversight or partial update in the variable management for this general configuration file.
    *   The code for the `omnitureEventManager` function appears truncated in the logs for `aws_test.js` and `modul-audiencies-omniture.js`, but the visible parts are consistent with the other files, suggesting the truncation is an artifact of the log rather than a functional difference.

**Patterns and Recurring Elements:**

*   **Adobe AppMeasurement 2.1.0**: All files utilize the same version of the Adobe Analytics tracking library.
*   **Standard Configuration**: Common settings across all files include:
    *   `charSet: 'UTF-8'`
    *   `currencyCode: 'EUR'`
    *   Link tracking enabled (`trackDownloadLinks`, `trackExternalLinks`, `trackInlineStats`) with consistent file types and internal filters.
    *   Daylight Saving Time (DST) definitions from 2016 to 2035.
*   **`s_doPlugins` Function**: This function is identical across all versions, responsible for:
    *   Capturing `campaign` (`ext`) and `eVar1` (`int`) query parameters.
    *   Setting `event1` for page views and `event2` for search events (if `eVar40` is defined).
    *   Conditionally pushing `eVar63`, `eVar71`, `eVar73` and their corresponding props (`prop63`, `prop71`, `prop73`) to `linkTrackVars` if `window.globalEvars` is available.
    *   Detecting the presence of the `VisitorAPI` and setting `s.prop70`.
    *   Identifying error pages (`isNotFound`).
*   **Media Module**: All files load the `Media` module and define extensive media tracking configurations, including `s.Media.trackVars`, `s.Media.trackEvents`, `s.Media.trackSeconds`, and `s.Media.contextDataMapping` for various media events (open, play, pause, stop, close, milestones).
*   **Helper Functions**: A set of utility JavaScript functions are consistently present across all files:
    *   `avoidEmptyEVar`, `avoidEmptyNumberEVar`: For normalizing empty or null eVar values.
    *   `getViewPortPage`: To determine the viewport size (Large, Regular, Small, Mobile) using `jitdevizr`.
    *   `getQueryString`: To extract the raw query string.
    *   `getQueryStringHash`: To parse specific parameters from the URL hash, particularly for `mrecInfo` (eVar64, eVar53) and `spmList`.
    *   `getLocalStorage`: To retrieve and clear a 'recomanats-modul' item from local storage.
    *   `getQueryStringTag`: To parse multiple 'name' attributes from a string.
    *   `getFont`: To map specific font codes (e.g., 'sACN' to 'ACN').
    *   `getClenaUrl`: To remove query parameters from a URL.
    *   `getTemporada`: To clean season strings.
*   **`omnitureEventManager`**: This central function manages different media events (OPEN, PLAY, PAUSE, STOP, CLOSE) by interacting with the `s.Media` object, updating internal state variables (`mediaAturat`, `omnitureIniciat`, `segonsIniciOmniture`, `segonsPosicioOmniture`), and clearing variables upon media close.