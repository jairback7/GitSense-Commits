# Activity Summary for 29/5/2026

## 10:26:32
The code changes primarily focus on enhancing the application's accessibility, refining video playback (Shaka Player) configurations, and improving the development and deployment infrastructure.

**File-Specific Updates:**

*   **`src\components\modals\exitAppModal\index.jsx` (18/5/2026, 18:25:30):**
    *   Introduced a kids-specific stylesheet (`styles.kids.scss`).
    *   Modified the active user context handling to be dismissible.
    *   Added conditional logic to display different exit messages based on the active user type (e.g., a specific message for "Super3" kid profiles exiting the protected environment).

*   **`src\views\onboarding\usersScreen\index.jsx` (18/5/2026, 18:26:19 - 18:28:08):**
    *   Initial update added `tabIndex={0}` and `role="button"` to username labels for accessibility, making them keyboard focusable.
    *   Immediately reverted this change (at 18:28:08), removing `tabIndex={0}` from the username labels, suggesting a re-evaluation of focus delegation for embedded text within already focusable containers.

*   **`src\views\onboarding\addUserScreen\index.jsx` (18/5/2026, 18:29:20 - 18:58:36):**
    *   Incrementally added accessibility attributes to action buttons, first adding `tabIndex={0}` (18:29:20), then `role='button'` (18:29:31), then re-adding `tabIndex={0}` (18:33:05).
    *   Introduced an accessibility announcement mechanism (using `useRef` and `useEffect` with `aria-live` element) to announce page entry (18:44:37).
    *   Refined this announcement, making it conditional (`initialAnnouncementDone` state) and integrating the message into the `aria-label` of the first interactive element (18:54:56).
    *   Later, the dedicated `div` for the announcement was removed, suggesting the announcement responsibility was shifted or streamlined (18:58:36).

*   **`index.html` (18/5/2026, 19:03:24 - 20:10:14):**
    *   Underwent several rapid changes and reversions related to the page `<title>`: initially simplified to "3Cat", then restored to a longer version, and even briefly attempted to prevent dynamic title changes via `Object.defineProperty`.
    *   Accessibility adjustments for the `splashScreen` div, adding and then removing `tabindex` and `aria-label`.
    *   A major accessibility change was the addition of `aria-hidden="true"` to the `<body>` tag (19:59:35), which could severely impact screen reader users if not properly managed by the application.
    *   Included a `MutationObserver` script (19:26:14) to dynamically hide specific iframes (e.g., Adobe tracking/consent iframes) from accessibility trees, which was later removed (19:28:43).
    *   A global `a11y-announce` div was briefly added for screen reader announcements (19:35:08), then removed (19:46:09).
    *   Script imports for analytics/tracking (e.g., `visitorAPI.min.js`, `omniture.js`, `comscore.min.js`) were removed (20:06:17), partially restored (20:08:44), and then fully restored (20:10:14).
    *   Updated the Shaka player library from `shaka-4-7-1.js` to `shaka.4.13.8.js`.

*   **`src\App.jsx` (18/5/2026, 19:12:15 - 19:34:40):**
    *   Removed direct `document.title` manipulation, aligning with `index.html` changes.
    *   Introduced a `hideA11yNoise` function to programmatically hide specific iframes for accessibility, initially called in `useEffect` (19:23:38). This function was later removed (19:26:04), as similar functionality was introduced in `index.html` via `MutationObserver` (which itself was removed).
    *   Re-introduced an `announceRef` and `useEffect` in the main `App` component to announce the app title ("3Cat") on load, moving this global announcement responsibility (19:31:40).

*   **`src\player\handlers\shakaPlayerHandler.js` (19/5/2026, 11:11:20 - 12:59:41):**
    *   Underwent extensive and iterative changes to Shaka Player configuration and event handling.
    *   Introduced ABR (Adaptive Bitrate) locking/unlocking logic during seeking and buffering.
    *   Added `failureCallback` for better error handling in Shaka's streaming configuration.
    *   Implemented `window.AppGlobal.sendRemoteLog` for player-related errors.
    *   Attempted to manage VTT text tracks, first by adding `textDisplayFactory` configuration (11:11:20), then commenting it out (11:33:19), then re-enabling (11:42:53).
    *   Briefly suppressed Shaka VTT parsing warnings (11:11:20), then commented out the suppression (11:45:25).
    *   Experimented with VTT caching (`_vttCache` and `registerResponseFilter`) to prevent redundant requests (12:10:49), which was later removed (12:38:51).
    *   Explicitly disabled Shaka's native text track handling via `manifest: { disableText: true }` (12:39:57), then experimented with manual external VTT subtitle injection (`addTextTrackAsync`) in `loadShakaContent` (12:46:16).
    *   Ultimately, `disableText` and the manual subtitle injection were commented out (12:49:19), indicating a return to Shaka's default text track management or further refinement.
    *   The `ensureAbrConfigured` method was refactored and moved for better lifecycle management of ABR settings (12:58:38, 12:59:41).

*   **`src\api\urlServices.js` (19/5/2026, 12:52:36):**
    *   Added a `MAX_RETRIES` constant for API calls.
    *   Enhanced API request headers with `X-Device-OS-Version` using a new `convertVersionTo_a_b_c` helper function for consistent version formatting.

*   **`src\player\handlers\shakaPlayerHandler.js` (19/5/2026, 12:58:38 - 12:59:41):**
    *   Finalized the ABR configuration setup, ensuring it's enabled and linked to a custom `AbrManager` for non-live/external streams.

*   **`package.json` (26/5/2026, 13:20:19 - 13:34:14):**
    *   Added `express`, `path`, and `url` to dependencies, indicating the introduction of a custom Node.js server.
    *   Expanded script commands significantly to support platform-specific and environment-specific builds and server executions (e.g., `build:smarttv:hosted_webapp:dev`, `server:hbbtv:prod`, `start:server:smarttv:dev`), improving the local development and deployment workflow.

*   **`server.js` (26/5/2026, 13:21:45 - 13:35:33):**
    *   Introduced an Express.js server to serve the hosted web application, dynamically constructing the `dist` path based on `PLATFORM` and `ENV` environment variables.
    *   Evolved routing from basic `*` to `/:path(.*)` and then to `/.*/` (regex) to handle SPA routing more robustly.
    *   Refined server startup logs, including using `console.table` for better status visibility.
    *   Changed the default server `PORT` from `3000` to `8080` (13:29:23).

*   **`src\views\page\index.jsx` (28/5/2026, 11:49:17 - 12:45:31):**
    *   Added logic to dynamically update slider items with live stream data from `ApiGetNowPlaying`.
    *   Implemented conditional fetching of live data (`ApiGetNowPlayingSelector`, `ApiGetNowPlaying`) based on `window.AppGlobal.isLite` flag, indicating performance optimization or feature toggling for a "Lite" mode.
    *   Included console logs for when live data fetching is canceled in "Lite" mode.

*   **`src\components\sideMenu\index.jsx` (28/5/2026, 11:52:40 - 15:18:02):**
    *   Centralized menu initialization and control through `window.AppMenu` global object.
    *   Enhanced focus management within the menu using `nav.focusById` and `nav.storedFocus`, particularly when navigating between menu levels or after modal interactions.
    *   Added `onMouseOver` handlers for menu items, improving responsiveness for pointer devices (though this is typically for smart TVs, likely indicating a hybrid input model).

*   **`src\components\spinner\index.jsx` (28/5/2026, 11:54:47 - 12:43:32):**
    *   Briefly added `aria-busy="true"` and `role="status"` to the spinner for accessibility (11:54:47), then removed it (12:28:15), indicating a possible design decision to handle loading state announcements elsewhere or differently.

*   **`src\components\modals\genericError\index.jsx` (28/5/2026, 11:58:17 - 12:29:51):**
    *   Adjusted `aria-live` attribute from `assertive` to `polite` (12:29:51) on the error modal, making accessibility announcements less interruptive.
    *   Removed `aria-hidden="true"` from the icon and added `tabIndex={-1}` to the close button, indicating a shift in accessibility focus management for interactive elements.

*   **`src\focus\focusable\index.jsx` (28/5/2026, 12:19:31):**
    *   Added `tabIndex='0'` to the wrapper div of the `withFocusable` HOC, making components wrapped with it natively keyboard focusable.

*   **`src\components\sideMenu\avatar\index.jsx` (28/5/2026, 12:21:45):**
    *   Added `aria-hidden="true"` to the character avatar for non-kid profiles, preventing redundant screen reader announcements when the username is already conveyed.

**Patterns and Recurring Elements:**

1.  **Accessibility (ARIA) Focus:** A consistent effort to refine accessibility attributes (`aria-label`, `aria-hidden`, `aria-live`, `role`, `tabIndex`) is observed across multiple UI components and the `index.html` file. This indicates a strong emphasis on making the application navigable and understandable for users with screen readers and keyboard-only navigation. There are multiple instances of adding, removing, and re-adding these attributes, suggesting an iterative process of testing and adjusting for optimal accessibility.
2.  **Custom Navigation/Focus System:** The frequent use of global `nav` object methods like `nav.focusById`, `nav.capture`, `nav.release`, `nav.storedFocus`, and `nav.fixedFocus` points to a custom-built navigation and focus management system, likely optimized for Smart TV environments.
3.  **Shaka Player Configuration Tweaks:** The `shakaPlayerHandler.js` file shows continuous fine-tuning of video playback parameters, including buffering, retry mechanisms, ABR (Adaptive Bitrate) logic, and text track (subtitle) handling. This indicates ongoing optimization for diverse streaming conditions and media types. The pattern of commenting out and re-implementing features (like VTT caching or `disableText`) suggests a cautious and experimental approach to player robustness.
4.  **Environmental and Build Process Refinement:** Updates to `package.json` and `server.js` demonstrate a maturation of the development workflow, introducing platform and environment-specific build commands and a dedicated Express server for local validation. This promotes more organized and scalable deployment strategies.
5.  **Global Object Usage:** The reliance on `window.AppGlobal` and `window.AppMenu` for shared actions, settings, and navigation state is a recurring pattern, indicating a centralized approach to application-wide data and functionality.
6.  **Iterative Development and Reversions:** Several changes involve adding a feature, then immediately modifying, commenting out, or completely reverting it in subsequent commits within a short timeframe. This suggests an agile development cycle with frequent testing and adjustments based on immediate feedback or further analysis (e.g., the `document.title` handling, splash screen accessibility, and many Shaka Player settings).