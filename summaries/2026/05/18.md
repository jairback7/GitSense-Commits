# Activity Summary for 18/5/2026

## 13:08:11
The provided log details a series of code changes occurring on **May 18, 2026**, between 11:38:44 and 12:58:23, indicating a focused development session. Key updates span application configuration, user onboarding, UI components, API service management, spatial navigation, and media playback.

Here's a breakdown of the key information:

**File-Specific Updates:**

*   **`c:\Users\Usuario\Documents\Trabajo\MEDIAPRO\3cat_smarttv\src\config.js` (11:38:44)**
    *   This file defines the application's core configuration, extensively using `import.meta.env` for environment-specific values like `ENV`, `version`, `buildNumber`, and `buildDate`.
    *   It sets a fixed `PROTOCOL` to 'https:' and configures `IMGPROXY_URL`.
    *   Detailed nested configurations are present for `pagesConf` (login, home including slider settings, personalized rows, teasers), `player` (force Shaka Player, debug mode), `ads` (debug, enabled flags), and `test_app` (numerous flags for displaying build info, console, user agent, version, platform details, phantom focus, user config, modals debug, environment, and bucket info).
    *   Further configurations include `test_player` settings for player override and ads info, `HIDE_PLAYER_CONTROLS_TIME`, `APP_MODE`, `APP_PLATFORM`, `APP_PLATFORM_VERSION`, `DEVICE_MANUFACTURER`, and `test_analytics_config` with `BASE_API_URL` and `WEB_URL` pointing to 'dev.3cat.website'.

*   **`c:\Users\Usuario\Documents\Trabajo\MEDIAPRO\3cat_smarttv\src\views\onboarding\welcomeScreen\index.jsx` (Multiple updates)**
    *   **11:53:10**: Initial implementation of the `WelcomeScreen` component, utilizing `react-router-dom` for navigation to `/qrAuthScreen` (login) and `/signupScreen` (register). It dynamically renders buttons based on `onboardingModals` data, setting `aria-label` for accessibility.
    *   **12:08:54**: A minor update changed the main div's role from `region` to `main` and clarified `ariaLabel` passing to the `Button` component.
    *   **12:23:26**: Introduced significant accessibility enhancements. This version added `useRef` and an `announce` callback to manage a visually hidden `aria-live='assertive'` region, specifically for Samsung devices, to announce button labels on `onMouseOver` and `onFocus`. It also refactored label retrieval.
    *   **12:30:44**: Partially reverted the extensive accessibility changes from the previous commit. The `aria-live` region and `announce` function were removed. However, it maintained a more descriptive `ariaLabel` for buttons, incorporating a prefix "Estàs a la pàgina de Onboarding de 3cat. Opció " for improved context.

*   **`c:\Users\Usuario\Documents\Trabajo\MEDIAPRO\3cat_smarttv\src\components\buttons\onboardingButton\index.jsx` (12:05:22)**
    *   This component defines a reusable `Button` with styling for onboarding. It includes `focusStyle`, `blockFocus` logic, and conditional `data-block-focus-*` attributes for custom focus navigation rules. It also uses `nav.focusById` on `onMouseOver`.

*   **`c:\Users\Usuario\Documents\Trabajo\MEDIAPRO\3cat_smarttv\src\focus\navigable.js` (12:31:41)**
    *   This file provides extensive internal documentation and implementation for a `spatial-navigator` (NAV) JS library.
    *   It details how focusable elements are identified and managed, including grouping (`data-focus-group-name`, `data-group`), actions (`data-link`, `data-action`), and various focus blocking/allowing/targeting attributes.
    *   The core logic (`focusItem`, `refreshListOfFocusableItems`, `refreshListOfFocusableGroups`) handles selecting, updating metadata, and applying the 'focused' class to elements, along with group memory for last-focused items.

*   **`c:\Users\Usuario\Documents\Trabajo\MEDIAPRO\3cat_smarttv\src\api\urlServices.js` (12:38:57)**
    *   This file centralizes API host and token URL management.
    *   It defines `DEV_SERVICES_HOST`, `PRE_SERVICES_HOST`, `PROD_SERVICES_HOST`, and `DUMMY_SERVICES_HOST` using 'https:'.
    *   Introduces `MAX_RETRIES` for API calls.
    *   `getServicesHost` dynamically selects the host based on `window.config.ENV`.
    *   `getTokenUrl` provides different token endpoints for QR code and password authentication.
    *   `getApiHeaders` constructs common headers including device type, OS, app version, and a conditional `X-Is-Child` header.
    *   Includes a `convertVersionTo_a_b_c` utility to format version strings.

*   **`c:\Users\Usuario\Documents\Trabajo\MEDIAPRO\3cat_smarttv\src\player\handlers\shakaPlayerHandler.js` (12:47:02)**
    *   This file implements a robust `ShakaPlayerHandler` for video and audio playback, extending a `BasePlayerHandler`.
    *   It defines distinct `SHAKA_WEB_CONFIG` and `SHAKA_TV_CONFIG` with varying `rebufferingGoal` and `bufferingGoal` for different environments.
    *   Manages content loading, seeking, live streams, and a fallback mechanism to MP4/MP3 for incompatible formats.
    *   Integrates deeply with an `adsManager` for pre-roll, mid-roll, and ad tracking, handling ad playback and content resumption.
    *   Implements detailed event handling for `canplay`, `playing`, `ended`, `timeupdate`, `waiting`, and `error` events, including displaying a buffering indicator.
    *   Includes methods to `lockABR` and `unlockABR` (Adaptive Bitrate) for Shaka Player, especially during seeking or buffering.
    *   Features error logging via `window.AppGlobal.sendRemoteLog` for playback and initialization errors.

*   **`c:\Users\Usuario\Documents\Trabajo\MEDIAPRO\3cat_smarttv\src\views\onboarding\signupScreen\index.jsx` (12:58:23)**
    *   This component facilitates user registration via a QR code.
    *   It fetches a `tvCode` from an API using `GetLoginCode`, which is then used to generate a QR code via `generateQR`.
    *   An `useEffect` hook sets up a polling mechanism (using `CheckLoginStatus`) to continuously check the login status until authenticated or max attempts are reached (triggering code regeneration).
    *   Upon successful authentication, it decodes the token, sets the active user, and navigates to the home screen or back.
    *   Includes UI elements to display the QR code, a web URL for validation (`tv_sign_up_url`), and the `tvCode`. A `BackButton` is also provided.

**Patterns and Recurring Elements:**

*   **Smart TV Focus**: Many changes, particularly in `config.js`, `shakaPlayerHandler.js`, `navigable.js`, and `welcomeScreen.jsx`, directly address the specific requirements and challenges of Smart TV environments, such as spatial navigation (`nav.focusById`), platform-specific player configurations, and accessibility considerations (e.g., Samsung screen reader quirks).
*   **Environment Configuration**: The application heavily leverages environment variables (`import.meta.env.VITE_ENV`) and a global `config` object to manage behavior and service endpoints across different development, preproduction, and production environments.
*   **Component-Based Architecture**: The use of reusable `Button` and `Text` components, alongside distinct screen components (`WelcomeScreen`, `SignupScreen`), indicates a structured, component-driven React application.
*   **Asynchronous Operations & Polling**: The `signupScreen.jsx` demonstrates active polling for API responses (`CheckLoginStatus`), which is a common pattern for scenarios like QR code authentication where an external action is awaited.
*   **Accessibility Iteration**: The `welcomeScreen.jsx` showed an iterative approach to accessibility, implementing advanced features (like `aria-live` regions) and then refining them, highlighting ongoing efforts to improve inclusivity.
*   **Robust Media Playback**: The `shakaPlayerHandler.js` indicates a commitment to a high-quality, adaptable media experience, including support for adaptive streaming, ad integration, and fallback mechanisms for various media formats.

## 15:08:27
The code changes log primarily covers updates to the `3cat_smarttv` application, focusing on onboarding flows, application configuration, API service definitions, player handling, and a new keyboard component. All changes occurred on **18/5/2026**, within a concentrated development period between 11:38:44 and 15:04:02.

**File-Specific Updates:**

*   **`src\config.js` (11:38:44):**
    *   Established a comprehensive application-wide configuration. This includes environment variables (`VITE_ENV`, `VITE_APP_VERSION`), image proxy URL, maximum row items, and feature flags for `isLite` mode, `pagesConf` (login bypass, home slider/personalized rows/teasers), `player` (Shaka Player forcing, debug), `ads` (debug, enabled), and numerous `test_app` and `test_player` debug display options. Analytics configuration (`test_analytics_config`) with environment-specific URLs is also defined.

*   **`src\views\onboarding\welcomeScreen\index.jsx` (11:53:10 - 12:30:44):**
    *   **11:53:10:** Initial implementation of the `WelcomeScreen` React component. It navigates to `/qrAuthScreen` for login and `/signupScreen` for registration, maps buttons from `onboarding_modals` (fetched from `window.AppGlobal.Settings`), and sets initial focus.
    *   **12:08:54:** Minor accessibility adjustments: `role="region"` changed to `role="main"` on the main view, a hidden `h1` was removed, and `ariaLabel` was explicitly passed to `Button`.
    *   **12:23:26:** Introduced significant accessibility enhancements for screen readers, particularly for Samsung devices. This involved adding `useRef` and `useCallback` for an `announce` function to manage an `aria-live="assertive"` region for button labels on focus/mouseover. `useText` labels were memoized.
    *   **12:30:44:** Reverted the complex live region accessibility implementation. The `announce` function and the hidden `aria-live` div were removed, and the `ariaLabel` for buttons was simplified to a fixed phrase.

*   **`src\components\buttons\onboardingButton\index.jsx` (12:05:22):**
    *   Initial implementation of a reusable `onboardingButton` component. It conditionally applies a `focusable` class, supports various `data-block-focus` attributes, and defaults `data-group` if not provided.

*   **`src\focus\navigable.js` (12:31:41):**
    *   A comprehensive spatial navigation library was introduced or significantly updated. This JavaScript code manages focusable elements, defines classes and data attributes for directional navigation (e.g., `BLOCK-FOCUS-UP`, `RIGHT-TARGET`), grouping, and storing/restoring last focused elements. Functions like `focusItem` handle the core logic of focus movement and state.

*   **`src\api\urlServices.js` (12:38:57):**
    *   Defined API service hosts for different environments (development, preproduction, production, dummy) based on `window.config.ENV`. It provides `getTokenUrl` for various authentication types and `getApiHeaders` that include device and application version details, and a child user indicator (`X-Is-Child`). A `convertVersionTo_a_b_c` utility was added for version string formatting.

*   **`src\player\handlers\shakaPlayerHandler.js` (12:47:02):**
    *   Implemented `ShakaPlayerHandler` for video/audio playback. This class integrates Shaka Player, handles media loading, play/pause/seek operations, and manages various player events (buffering, error, playing, etc.). It includes specific configurations for web and TV modes, ad management integration (`adsManager`), error fallback mechanisms (to MP4/MP3), and Adaptive Bitrate (ABR) locking/unlocking during critical playback states.

*   **`src\views\onboarding\signupScreen\index.jsx` (12:58:23 - 13:59:33):**
    *   **12:58:23:** Initial implementation of the `SignupScreen` for QR-code based registration. It fetches a `tvCode`, generates a QR code, and polls the API (`CheckLoginStatus`) for authentication. On success, it sets the user and navigates. Polling failures lead to code regeneration. `nav.focusById('btnBack')` is set initially.
    *   **13:12:35 - 13:36:31:** Successive refinements to the `ariaLabel` of the `BackButton` for improved screen reader clarity, dynamically including URLs and current `tvCode`.
    *   **13:18:54:** Added `aria-live="polite"` to the `tvCode` display and included the `tvCode` in the `BackButton`'s `ariaLabel`.
    *   **13:40:37:** Refactored `useEffect` to ensure `nav.focusById("btnBack")` is called *after* `tvCode` is set, for more reliable focus management.
    *   **13:43:44:** Made `BackButton`'s `ariaLabel` conditional, displaying "carregant..." (loading) if `tvCode` is not yet available.
    *   **13:44:32:** Removed `aria-live="polite"` from the `tvCode` display and moved it to the `BackButton`'s `ariaLabel` attribute.
    *   **13:51:17:** Corrected a typo (`ariaL abel`) and refined the `ariaLabel` text formatting.
    *   **13:56:00:** Reverted a previous focus management change, moving `nav.focusById('btnBack')` back to the initial `useEffect` that generates the QR code and sets up the refresh interval.

*   **`src\views\onboarding\qrAuthScreen\index.jsx` (14:05:17 - 14:36:06):**
    *   **14:05:17:** Initial implementation of the `QRAuthScreen` for QR-code based login. Similar to `SignupScreen`, it fetches a `tvCode`, generates a QR code, polls for status, authenticates users, and navigates. It also provides an option to switch to password authentication. Initial focus is on the password auth button.
    *   **14:05:52:** Added `aria-live="polite"` to the `tvCode` display for accessibility.
    *   **14:13:06:** Refactored `useEffect` to ensure `nav.focusById('btnGoToPasswordAuth')` is called *after* `tvCode` is set.
    *   **14:17:15:** Added `aria-live="polite"` to the password authentication button.
    *   **14:17:36, 14:31:47 (and subsequent reversions):** Repeatedly changed the QR code `setInterval` duration for `getCode()` between a dynamic `settingsData` value and hardcoded values (10 seconds, then 25 seconds). This was consistently reverted to use `settingsData` again.
    *   **14:23:25:** Removed `aria-live="polite"` from the `tvCode` display.

*   **`src\components\buttons\backButton\index.jsx` (13:12:56):**
    *   Integrated parental control context to determine if the button should be `focusable`. Added a `data-block-focus-up` attribute for navigation.

*   **`src\components\keyboard\index.jsx` (15:03:43 - 15:04:02):**
    *   **15:03:43:** Initial implementation of a virtual `KeyboardLayout` component. It manages `letters` and `symbols` modes, `CAPITALS_ON` state, and renders dynamic keys. `handleClick` handles key logic (caps lock, mode switch, input), and `getAriaLabel` provides accessibility text for special keys.

**Patterns and Recurring Elements:**

*   **Accessibility Focus:** A significant portion of the changes revolves around enhancing accessibility, particularly through `aria-label` and `aria-live` attributes, and adapting for specific device behaviors (e.g., Samsung's live region handling). There's an iterative process of adding, refining, and sometimes reverting these attributes.
*   **Spatial Navigation:** The `nav.focusById()` function from the `navigable.js` library is a key element, used across multiple UI components to manage focus in a smart TV environment.
*   **QR Code Authentication Flow:** Both `SignupScreen` and `QRAuthScreen` implement a similar flow involving generating a `tvCode` and QR code, polling an API for authentication status, and handling timeouts/errors.
*   **Dynamic vs. Hardcoded Intervals:** A recurring pattern in the authentication screens is the frequent change and reversion of the QR code regeneration `setInterval` duration between a dynamic value from `settingsData` and hardcoded seconds.
*   **Component-Based Development:** The file structure and content consistently reflect a component-based React application, with distinct components for buttons, screens, and utilities.
*   **Environment-Specific Configuration:** The application uses `import.meta.env` and `window.config.ENV` for tailoring behavior and API endpoints based on the deployment environment.

## 16:09:20
The provided log details a series of code changes primarily focused on the onboarding process, UI components, and player functionality for a 3cat smartTV application, with a strong emphasis on accessibility and spatial navigation. The timestamps span from 11:38:44 to 16:05:57 on 18/5/2026, indicating concentrated development activity.

**File-Specific Updates:**

*   **`c:\Users\Usuario\Documents\Trabajo\MEDIAPRO\3cat_smarttv\src\config.js` (18/5/2026, 11:38:44):**
    *   This initial commit establishes the core application configuration, including environment variables (e.g., `VITE_ENV`, `VITE_APP_VERSION`), service URLs (`IMGPROXY_URL`, `BASE_API_URL` for analytics), and numerous feature flags.
    *   It defines settings for player behavior (e.g., `forceShakaPlayer`, `playerDebug`), ad integration (e.g., `enableAds`, `adsDebug`), and a comprehensive `test_app` section with flags for showing build information, console, user agent, resolution, and debug modals.
    *   A `parseEnvBool` utility is provided to convert environment variable strings to booleans.

*   **`c:\Users\Usuario\Documents\Trabajo\MEDIAPRO\3cat_smarttv\src\views\onboarding\welcomeScreen\index.jsx` (multiple changes from 18/5/2026, 11:53:10 to 12:30:44):**
    *   **Initial Setup (11:53:10):** Implemented the `WelcomeScreen` React component for user onboarding, dynamically rendering "Sign In" and "Register" buttons based on `onboardingModals` data. It uses `useNavigate` to direct to `/qrAuthScreen` and `/signupScreen`. Initial focus is set using `nav.focusById`.
    *   **Accessibility Iterations (12:08:54, 12:23:26, 12:30:44):** Early changes involved switching a `role="region"` to `role="main"` and refining `aria-label` attributes for buttons. A significant update introduced an `aria-live="assertive"` region and an `announce` function (12:23:26) to provide dynamic screen reader feedback on button focus, specifically mentioning Samsung compatibility. This `aria-live` region was subsequently removed, with accessibility relying instead on more descriptive `ariaLabel` props passed to the buttons, including context like "Estàs a la pàgina de Onboarding de 3cat" (12:30:44).

*   **`c:\Users\Usuario\Documents\Trabajo\MEDIAPRO\3cat_smarttv\src\components\buttons\onboardingButton\index.jsx` (18/5/2026, 12:05:22):**
    *   Introduced a reusable `OnboardingButton` component, centralizing button styling and spatial navigation attributes (e.g., `focusStyle`, `data-block-focus-left`). It ensures a `data-group` attribute is present.

*   **`c:\Users\Usuario\Documents\Trabajo\MEDIAPRO\3cat_smarttv\src\focus\navigable.js` (18/5/2026, 12:31:41):**
    *   This file outlines the internal workings of the spatial navigation library. It details numerous data attributes for defining focus groups, blocking focus movement, setting focus targets, and executing actions on focus events. It includes logic for managing lists of focusable items and groups, and for remembering the last focused element within a group.

*   **`c:\Users\Usuario\Documents\Trabajo\MEDIAPRO\3cat_smarttv\src\api\urlServices.js` (18/5/2026, 12:38:57):**
    *   Defines dynamic service hosts (`DEV_SERVICES_HOST`, `PRE_SERVICES_HOST`, `PROD_SERVICES_HOST`, `DUMMY_SERVICES_HOST`) based on the application's environment.
    *   Provides functions (`getTokenUrl`, `getApiHeaders`) for authenticating users and constructing API request headers, including device and user-type information.
    *   Adds a `convertVersionTo_a_b_c` utility for normalizing version strings.

*   **`c:\Users\Usuario\Documents\Trabajo\MEDIAPRO\3cat_smarttv\src\player\handlers\shakaPlayerHandler.js` (18/5/2026, 12:47:02):**
    *   Implements `ShakaPlayerHandler` for media playback, extending a `BasePlayerHandler`.
    *   Configures Shaka Player with specific streaming parameters for web and TV, including rebuffering and buffering goals.
    *   Handles content loading, playback, seeking, and manages various player events (e.g., `canplay`, `playing`, `ended`, `error`).
    *   Integrates ad management (`adsManager`) and includes a fallback mechanism for incompatible media formats (e.g., trying MP4/MP3) upon error. It also logs player errors remotely.
    *   Includes logic to lock/unlock ABR during seeking to improve user experience.

*   **`c:\Users\Usuario\Documents\Trabajo\MEDIAPRO\3cat_smarttv\src\views\onboarding\signupScreen\index.jsx` (multiple changes from 18/5/2026, 12:58:23 to 13:59:33):**
    *   **Initial Setup (12:58:23):** Developed the `SignupScreen` component for user registration via QR code, including fetching codes, generating QR images, and polling for authentication status.
    *   **Accessibility Refinements (13:12:35, 13:15:05, 13:18:54, 13:36:31, 13:43:44, 13:44:32, 13:51:17, 13:54:51, 13:56:00, 13:59:33):** Primarily focused on enhancing the `BackButton`'s `ariaLabel` for screen readers. This involved dynamically including the signup URL and `tvCode` in the label, and conditionally displaying "carregant..." (loading) before the `tvCode` is available. There was an instance where `aria-live="polite"` was moved from the `tvCode` display to the `BackButton` (13:44:32), potentially impacting how updates are announced. The initial focus logic for the `BackButton` was also adjusted multiple times.

*   **`c:\Users\Usuario\Documents\Trabajo\MEDIAPRO\3cat_smarttv\src\components\buttons\backButton\index.jsx` (18/5/2026, 13:12:56):**
    *   Modified the `BackButton` component to accept a dynamic `ariaLabel` prop and added a `data-block-focus-up` attribute for spatial navigation control.

*   **`c:\Users\Usuario\Documents\Trabajo\MEDIAPRO\3cat_smarttv\src\views\onboarding\qrAuthScreen\index.jsx` (multiple changes from 18/5/2026, 14:05:17 to 14:36:06):**
    *   **Initial Setup (14:05:17):** Implemented the `QRAuthScreen` component for QR-code-based login, sharing much of the authentication and polling logic with `SignupScreen`. It provides a way to navigate to a password authentication screen.
    *   **Accessibility & Polling Interval Adjustments (14:05:52, 14:13:06, 14:17:15, 14:17:36, 14:20:52, 14:31:47, 14:36:06):** Similar to `SignupScreen`, `aria-live="polite"` was added to the `tvCode` display (14:05:52) and then moved to the `btnGoToPasswordAuth` (14:17:15). There were repeated changes and reversions regarding the QR code regeneration `setInterval` duration, alternating between a hardcoded value (10s at 14:17:36, 25s at 14:31:47) and using `settingsData?.tv_token_refresh_interval_seconds`. Initial focus was also adjusted to be set after `tvCode` is available.

*   **`c:\Users\Usuario\Documents\Trabajo\MEDIAPRO\3cat_smarttv\src\components\keyboard\index.jsx` (multiple changes from 18/5/2026, 15:03:43 to 16:05:57):**
    *   **Initial Setup (15:03:43):** Introduced a `KeyboardLayout` component for a virtual keyboard, supporting letter and symbol modes, and caps lock. It uses `aria-label` for accessibility and `nav.focusById` for key focus.
    *   **Functional & Focus Logic (15:12:13, 15:13:38, 15:15:32, 15:25:32, 15:26:26, 15:31:03, 15:31:56, 15:35:15, 15:38:00, 15:50:03, 15:51:57, 15:55:16, 15:57:37, 15:58:34, 16:02:45, 16:05:17):**
        *   Refined `getDisplayKey` to handle display text for mode-switching keys (Caps Lock, Symbols) (15:12:13).
        *   Improved caps lock behavior: pressing Caps Lock in symbol mode now switches to letter mode (15:15:32).
        *   Introduced and then removed `lastFocusedKey` (15:25:32-15:50:03) in favor of consistently focusing the *second* key of the top row (15:51:57) when `keyboardMode` or `CAPITALS_ON` changes, ensuring a predictable focus point.
        *   Added `lastInputType` state to differentiate between mouse and remote control input (15:55:16), preventing unwanted remote focus shifts when a mouse is used.
        *   Corrected event object passing to `handleClick` (16:05:17) to accurately determine input type.

*   **`c:\Users\Usuario\Documents\Trabajo\MEDIAPRO\3cat_smarttv\src\components\keyboard\keys.js` (18/5/2026, 15:11:20):**
    *   Defines the complete layout for the virtual keyboard, separating `mainKeys` (letters/numbers) and `symbols`. Each key object includes its value, ID, type, description, icon, and size. It includes special keys like `SYMBOLS_KEY`, `CAPS_LOCK_KEY`, `DELETE_KEY`, `SPACE_BAR_KEY`, `RETURN_KEY`, and `DELETE_FULL_TEXT_KEY`.

**Patterns and Recurring Elements:**

1.  **Accessibility (Aria Attributes):** A prominent pattern is the continuous refinement of accessibility, specifically using `aria-label` for descriptive text on interactive elements and experimentation with `aria-live="polite"` for dynamic announcements. This suggests a dedicated effort to improve usability for screen reader users on Smart TV platforms.
2.  **Spatial Navigation Integration:** The global `nav` object and its `focusById` method are heavily utilized across all UI components (`WelcomeScreen`, `SignupScreen`, `QRAuthScreen`, `OnboardingButton`, `BackButton`, `KeyboardLayout`), underscoring the importance of remote control navigation for a smart TV application. The `navigable.js` file highlights the complexity and custom nature of this navigation system.
3.  **QR Code Based Authentication:** The `SignupScreen` and `QRAuthScreen` share a common architecture for generating QR codes and polling for login status, indicating this as a primary authentication method.
4.  **Configuration-Driven Behavior:** Extensive use of `window.config` and `window.AppGlobal.Settings` for controlling features, environment, and API endpoints, allowing for flexible deployments and feature toggling.
5.  **React Hooks:** Consistent use of standard React hooks (`useEffect`, `useState`, `useRef`, `useCallback`, `useMemo`) across all functional components for state management, side effects, and memoization.
6.  **Iterative Refinement:** Many files, particularly the UI components (`WelcomeScreen`, `SignupScreen`, `QRAuthScreen`, `KeyboardLayout`), show multiple small, iterative changes. This is evident in the repeated adjustments to `ariaLabel` content, focus management logic, and the polling intervals for QR authentication, suggesting ongoing testing, bug fixing, and optimization.
7.  **Error Logging:** Integration of `console.error` and `window.AppGlobal.sendRemoteLog` in critical areas like player handlers and authentication flows, indicating a focus on monitoring and debugging issues in a production environment.